import type { MockingFormSchema } from '../../types/plugins/mocking'

export const mockingSchema: MockingFormSchema = {
  'config-api_specification': {
    label: 'Config.API Specification',
    placeholder: 'Enter an API spec',
    type: 'textArea',
    rows: 10,
  },
}
